window.angular.module('ERP').controller('AccountancyExchangeRatesCtrl', [
    '$scope', '$window', 'Page', 'Ajax',
    function ($scope, $window, Page, Ajax) {
        var angular = $window.angular,
            $ = angular.element;

        Page.title('Tipo de cambio');
    }
]);